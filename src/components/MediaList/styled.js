import InfiniteScroll from 'react-infinite-scroller';
import styled from 'styled-components';

export const MediaListContainer = styled(InfiniteScroll)`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  padding: .5rem;
  margin-bottom: 5rem;
`;
